name: image_zoomer
description: A new Flutter package for a customized Image Zoom in/out Dialog.
version: 0.0.2
homepage: https://github.com/yashprovis/image_zoomer

environment:
  sdk: '>=2.19.0 <3.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter: